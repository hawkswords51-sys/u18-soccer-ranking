# u18-soccer-ranking 運営メモ（Claude Code用）

## このリポジトリ
- 全国U-18サッカーのリーグ・大会情報サイト https://u18-soccer.com
- 運営者 Kei（救急科専門医・非エンジニア）。専門用語は避け、素人にわかる言葉で説明すること。
- 運営ルールの正本は別フォルダの「運営マスター手順書.md」
  (~/Documents/Claude/Projects/u18-soccer.com運営/運営マスター手順書.md)。判断に迷ったら読む。

## Claude Codeに任せている範囲（2026-08時点）
**チームページ（data/team-profiles/*.md）の公開作業のみ。**
速報更新・スクリプト改修・ブログ記事はCowork側で行う。
範囲外の作業を頼まれたら、実行前に「これはCowork側の範囲ですが進めますか」と確認する。

## 公開の仕組み
- data/team-profiles/** を編集して main に push すると、GitHub Actions が自動で
  ページを再生成してデプロイする。**手動Runは不要。** 反映まで2〜3分。
- push起動時はスクレイピングをスキップし、ページ再生成とコミットだけが走る。
- Actionsが赤なら push が届いていない。緑なら反映済みと信じてよい。

## 絶対に守ること
1. **生成スクリプトをリポジトリ内で実行しない。** generate_team_pages.py 等をリポジトリ直下で
   走らせると sitemap.xml を壊す。検証はリポジトリを /tmp にコピーしてそちらで行う。
2. **git add -A を使わない。** 意図したファイルだけを個別に add する
   （過去に検証用の一時ファイルがコミットに混入した事故あり）。
3. **git push --force は絶対に使わない。**
4. **ファイル削除は必ず事前にKeiに確認する。**
5. push前に git status を見せ、「これをコミットします」とKeiの承認を取ってから実行する。

## チームページの公開前チェック（毎回）
- frontmatter の YAML が壊れていないか。`name:` は data/teams.json の表記と完全一致か。
- 本文に書いてある創部年・監督名などが frontmatter にも入っているか。
- 本文の見出しは `##` から始める（`#` は自動で h2 に降格されるが `##` が正）。
- 検算：勝点＝勝×3＋分、得失点差＝得点−失点。
- 順位表と食い違うときは JFA の個別試合ページ（m番号）が正。
  JFAの星取表PDFは節が古いままのことがあるので根拠に使わない。

## 事実確認のルール（サイトの信頼性の根幹）
- 出典は必ず公式ページ本体で確認する。調査資料（docx/PDF）の主張は誤りが多い。
- 頻出の誤り：OBの他校混入・中学のみ在籍・現所属の陳腐化、野球部など他部の実績との混同、
  「優勝」と「全国出場」の取り違え、年度と暦年の取り違え。
- 確認できない事実は載せない。推測で補わない。

## 自サイトの実物確認
- キャッシュで古い版が返ることがある。確認時はURLに `?nocache=日付` を付ける。
